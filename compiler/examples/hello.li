\ hello.li — minimal Lithos function: vector add c[i] = a[i] + b[i]
\ This is the simplest possible Lithos program: one function, one operation.

fn vadd a b -> c
    each i
        c [ i ] = a [ i ] + b [ i ]
